# Sample workflow for building and deploying a Next.js site to a remote server via Docker and SSH

name: Deploy Next.js site to Pages

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  # Build job
  build-and-test:
    runs-on: ubuntu-latest
    env:
      AMOCRM_CLIENT_ID: ${{ secrets.AMOCRM_CLIENT_ID }}
      AMOCRM_CLIENT_SECRET: ${{ secrets.AMOCRM_CLIENT_SECRET }}
      AMOCRM_SUBDOMAIN: ${{ secrets.AMOCRM_SUBDOMAIN }}
      AMOCRM_REDIRECT_URI: ${{ secrets.AMOCRM_REDIRECT_URI }}
      AMOCRM_AUTHORIZATION_CODE: ${{ secrets.AMOCRM_AUTHORIZATION_CODE }}
      AMOCRM_ACCESS_TOKEN: ${{ secrets.AMOCRM_ACCESS_TOKEN }}
      AMOCRM_REFRESH_TOKEN: ${{ secrets.AMOCRM_REFRESH_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect package manager
        id: detect-package-manager
        run: |
          if [ -f "${{ github.workspace }}/yarn.lock" ]; then
            echo "manager=yarn" >> $GITHUB_OUTPUT
            echo "installer=install" >> $GITHUB_OUTPUT
            echo "builder=build" >> $GITHUB_OUTPUT
            exit 0
          elif [ -f "${{ github.workspace }}/package.json" ]; then
            echo "manager=npm" >> $GITHUB_OUTPUT
            echo "installer=install" >> $GITHUB_OUTPUT
            echo "builder=run build" >> $GITHUB_OUTPUT
            exit 0
          else
            echo "Unable to determine package manager"
            exit 1
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: ${{ steps.detect-package-manager.outputs.manager }}

      - name: 📦 Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential

      - name: 🔧 Clean install deps and rebuild native modules
        run: |
          rm -rf node_modules package-lock.json .next
          ${{ steps.detect-package-manager.outputs.manager }} ${{ steps.detect-package-manager.outputs.installer }} --legacy-peer-deps
          npm rebuild sharp lightningcss

      - name: 🚧 Build Next.js app
        run: ${{ steps.detect-package-manager.outputs.manager }} ${{ steps.detect-package-manager.outputs.builder }}

  # Deploy job
  deploy:
    runs-on: ubuntu-latest
    needs: build-and-test
    # if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 🚀 Deploy to Server via SSH and Docker
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          SERVER_IP: ${{ secrets.SERVER_IP }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          DEPLOY_LOCATION: ${{ secrets.DEPLOY_LOCATION }}
          GIT_REPO: git@github.com:${{ github.repository }}.git
        run: |

          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H $SERVER_IP >> ~/.ssh/known_hosts

          ssh $SERVER_USER@$SERVER_IP << EOF
            set -euo pipefail
            export DEPLOY_DIR="/var/www/president"

            echo "🔐 Connected to production server"

            # Clone or pull repo
            if [ -d "$DEPLOY_DIR" ] && [ -d "$DEPLOY_DIR/.git" ]; then
              echo "📥 Pulling latest repository..."
              cd $DEPLOY_DIR
              git pull origin main
            else
              echo "📦 Cloning repository... $GIT_REPO"
              rm -rf $DEPLOY_DIR  # Remove incomplete directory if it exists
              git clone $GIT_REPO $DEPLOY_DIR
            fi

            cd $DEPLOY_DIR

            # Install Docker if missing
            if ! command -v docker &> /dev/null; then
              echo "🐳 Installing Docker..."
              curl -fsSL https://get.docker.com -o get-docker.sh
              sudo sh get-docker.sh
            fi

            echo "✅ Docker is installed"

            # Install Docker Compose plugin if missing
            if ! docker compose version &> /dev/null; then
              echo "🧩 Installing Docker Compose plugin..."
              sudo mkdir -p /usr/local/lib/docker/cli-plugins
              sudo curl -SL "https://github.com/docker/compose/releases/latest/download/docker-compose-linux-\$(uname -m)" -o /usr/local/lib/docker/cli-plugins/docker-compose
              sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
            fi

            echo "✅ Docker Compose is installed"

            echo "🛑 Stopping old containers (if running)..."
            docker compose down || true
            
            echo "🧱 Building containers..."
            docker compose build

            echo "🚀 Starting containers..."
            docker compose up -d --remove-orphans  

            echo "🧹 Optional cleanup of dangling images..."
            docker image prune -f

            echo "📊 Deployment status:"
            docker compose ps
           
            echo "✅ Production deployment complete at \$(date)"
          EOF
